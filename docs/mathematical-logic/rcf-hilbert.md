# QE for RCF and Hilbert 17th Problem

We based on $\mathscr L_{oring}$ in the following.



RCF: The theory of real closed fields.

+ Axiom of fields, $<$ is a strict linear ordering.

  Compativility axiom

  $x < y \to x + z < y + z$

  $x < y \land 0 < z \to x z < y z$

+ $\forall x, x >0 \to \exists y, x = y^2$ (Every element > 0 has a square root)

+ Every odd-defree polynomial has a solution.

